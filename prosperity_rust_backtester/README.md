i vendor the rust prosperity backtester here for r4 replays. i only depend on `datasets/round4/` in this repo snapshot.

```bash
make round4 TRADER=../trader.py
```

upstream lives on github `GeyzsoN/prosperity_rust_backtester` if i need the full upstream readme.
