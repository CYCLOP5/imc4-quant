r2 analysis
===========

i ran loaders + characterize + compare vs r1 + signals + kalman grid + maf scaling replays here. outputs: `r2_characterize.csv`, `r1_vs_r2.csv`, `plots/`, etc. run from this folder with python; paths use `prosperity_rust_backtester/datasets/round2/`.
