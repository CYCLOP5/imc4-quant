r3 analysis
===========

i put my csv dumps and one-off scripts here: regime stats (`characterize.py`), r2 vs r3 compare, iv smile fits, voucher basis, official parse helpers, sweep. outputs are the various `r3_*.csv` files in this folder. run scripts from repo root; paths assume `prosperity_rust_backtester/datasets/round3/`.
